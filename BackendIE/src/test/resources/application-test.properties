# Test profile properties: use H2 for JPA and embedded Mongo for MongoDB

DB_PASSWORD=1234
TOKEN_SECRET=UcpcTUB8LdxR08M4DXc4x/ivUzrA2g2xz/lGPWTs+cdM4jNJLwMW4A==
FRONTEND_URL=http://localhost:3001
EMAIL_PASSWORD=maui ybwq wsuu mnbr
EMAIL_USERNAME=duvanvch12@gmail.com
EMAIL_FROM=duvanvch12@gmail.com


# Use H2 in-memory for JPA
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=postgres
spring.datasource.password=1234
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# Embedded Mongo (Flapdoodle will start automatically)
spring.data.mongodb.port=27017
spring.data.mongodb.database=auditoria_db
# Flapdoodle embedded mongo version
spring.mongodb.embedded.version=6.0.5

# Disable external services during tests
spring.ai.ollama.base-url=http://localhost:11434
spring.ai.ollama.chat.model=llama3.1:8b

# Mail placeholders required by EmailService (provide safe test defaults)
email.from=${EMAIL_FROM}
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME}
spring.mail.password=${EMAIL_PASSWORD}

# Frontend URL used in emails
frontend.url=${FRONTEND_URL}

token.secret=${TOKEN_SECRET}

# Reduce log during tests
logging.level.org.springframework=INFO
logging.level.com.backendie=DEBUG

# Ensure web environment for MockMvc tests
spring.main.web-application-type=servlet
